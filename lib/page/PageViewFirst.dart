import 'package:flutter/material.dart';

class PageViewFirst extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("PageViewFirst"),
    );
  }

}