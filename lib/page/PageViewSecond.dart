import 'package:flutter/material.dart';

class PageViewSecond extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("PageViewSecond"),
    );
  }

}